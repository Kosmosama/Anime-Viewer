package animelist.interfaces;

/**
 * Interface for objects that can be filtered.
 */
public interface IFilterable {
    /**
     * Method to filter the list.
     * Implementations of this method should perform filtering operations on the list.
     */
    void filterList();
}
